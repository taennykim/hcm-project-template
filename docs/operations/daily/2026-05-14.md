# 2026-05-14

## 오늘 작업
- HCM-007.1 Attendance Input Bootstrap task 생성
- HCM-007 backlog item 생성 기준 정리
- HCM-007은 아직 구현 전이며 다음 단계는 HCM-007.2 in-progress 이동으로 기록
- HCM-007.2 in-progress 이동
- HCM-007.3 Attendance API 구현
- HCM-007.4 Attendance UI 구현
- HCM-007.5 검증 수행
- Attendance in-memory persistence 유지
- 월 근태 집계/급여 계산은 이번 범위에서 제외
- `python3 -m py_compile` 통과
- `cd apps/web && npm install && npm run build` 완료
- `/attendance` route build 산출 확인
- FastAPI app import/service 직접 호출 검증은 로컬 Python 의존성 부재로 불가
- `.venv-verify` 생성 및 `pip install -r apps/api/requirements.txt` 시도했으나 네트워크 제한으로 실패
- Docker socket 권한 부족으로 `docker compose up -d --build api web nginx` 실행 불가
- 이 세션 기준 `curl http://localhost/...` 연결 불가, 대체 검증 수행
- web 502 원인 점검
- `apps/web/package.json` start script를 host/port 명시 형태로 수정
- `infra/docker/docker-compose.yml` web command 및 `HOSTNAME`/`PORT` 환경변수 명시
- HCM-007.6 Design Sync 점검 완료
- HCM-007.7 done 처리 준비 완료
- HCM-007 Attendance Input Bootstrap 완료
- 사용자 Docker/curl 검증 정상 확인
- web 502 수정 후 정상화
- 다음 작업 후보: HCM-008 Monthly Attendance Summary Bootstrap
- HCM-008.1 Monthly Attendance Summary Bootstrap task 생성
- HCM-008은 아직 구현 전이며 다음 단계는 HCM-008.2 in-progress 이동으로 기록
- HCM-008.2 in-progress 이동
- HCM-008.3 Monthly Attendance Summary API 구현
- HCM-008.4 Monthly Attendance Summary UI 구현
- HCM-008.5 Codex 가능 범위 검증 수행
- Monthly Attendance Summary in-memory persistence 유지
- 급여 계산은 이번 범위에서 제외
- 사용자 Docker/curl 검증 필요
- `apps/api/app/domains/monthly_attendance/` 구현 및 main router 등록
- `/attendance/monthly-summary` route 구현
- `docs/domain/domain-model.md`, `docs/data/erd.md`, `docs/product/menu-structure.md`, `docs/operations/smoke-test.md` 보강
- `python3 -m py_compile ...monthly_attendance...` 통과
- `cd apps/web && npm install && npm run build` 통과
- `/attendance/monthly-summary` route build 산출 확인
- FastAPI app import/service 직접 호출 검증은 로컬 Python에 `fastapi` 부재로 불가
- 사용자 Docker/curl 검증에서 `/api/monthly-attendance-summaries` 및 generate API 정상 확인
- `/attendance/monthly-summary` 502 원인은 nginx upstream DNS 캐시 문제로 확인
- `infra/docker/nginx/default.conf`에 Docker DNS resolver(`127.0.0.11`) 적용으로 web/api/mock-server upstream 재해석 가능하게 수정
- `/attendance/monthly-summary` UI 정상 확인
- HCM-008.6 Design Sync 점검 완료
- HCM-008.7 done 처리 완료
- HCM-008 Monthly Attendance Summary Bootstrap 완료
- 다음 작업 후보: HCM-009 Payroll Run Bootstrap
- HCM-009.1 Payroll Run Bootstrap task 생성
- HCM-009.2 in-progress 이동
- HCM-009.3 Payroll Run API 구현
- HCM-009.4 Payroll Run UI 구현
- HCM-009.5 Codex 가능 범위 검증 수행
- Payroll Run / Payroll Item in-memory persistence 유지
- 세법/4대보험/원천세/급여명세서 발행은 HCM-009 범위에서 제외
- 제외된 세법/4대보험/원천세/외부연동은 후속 task에서 단계적 구현 가능
- 사용자 Docker/curl 검증 필요
- `apps/api/app/domains/payroll/` 구현 및 main router 등록
- `/payroll` route 구현
- `python3 -m py_compile ...payroll...` 통과
- `cd apps/web && npm install && npm run build` 통과
- `/payroll` route build 산출 확인
- FastAPI app import/service 직접 호출 검증은 로컬 Python에 `fastapi` 부재로 불가
- HCM-009.6 Design Sync 점검 완료
- HCM-009.7 done 처리 완료
- HCM-009 Payroll Run Bootstrap 완료
- 사용자 Docker/curl 검증 정상 확인
- 다음 작업 후보: HCM-010 Payslip Bootstrap
- Company REAL route 404 원인 확인: Company API는 HCM-003에서 구현됐지만 `/company` UI route가 없어 navigation과 실제 route가 불일치함
- `/company` UI 구현으로 navigation REAL route 정합성 보정
- `/api/companies/dev-company`, `/api/companies/dev-company/policy` 기반 Company 화면 추가
- 복잡한 정책 편집 UI는 제외하고 조회 화면만 구현

## 진행 상태
- HCM-007 done
- HCM-008 done
- HCM-009 done

## 이슈
- Attendance 데이터는 현재 in-memory persistence로 유지
- 월 근태 집계/급여 계산/급여명세서/DB migration/PostgreSQL persistence 전환은 제외
- Docker Compose 구조 변경 없음
- 로컬 Python에 `fastapi`, `pydantic`가 없어 app import 기반 검증은 직접 수행하지 못함

## 다음 작업
- HCM-010 Payslip Bootstrap
